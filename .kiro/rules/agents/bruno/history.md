# Bruno History

- Owner de API CRUD, Prisma, auditoria y persistencia del sistema.
- Modulo `activities` se usa como blueprint CRUD con validaciones y pruebas verificadas.
- `departament` quedo dividido en CRUD de departamentos, cargos y empresas de servicios con endpoints dedicados.
- En tarifas se validaron APIs multiorg en `/api/forest/tariffs/scenarios` y `/api/forest/tariffs/catalog`.